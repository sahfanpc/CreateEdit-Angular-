import { NumberGroupDirective } from './number-group.directive';

describe('NumberGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberGroupDirective();
    expect(directive).toBeTruthy();
  });
});
